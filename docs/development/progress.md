# Implementation progress

Updated: 2026-09-22. Status: active evaluation and implementation.

## Current objective

Build the first complete release by the [roadmap build order](../../ROADMAP.md#build-order), one verified operation at a time. Operations 1 through 22 have their recorded bounded evidence below. Operation 23 has legacy transcript persistence groundwork and supervised input verification, but it does not recognize speech. New transcribe requests now fail without creating empty cues. Operation 24 now has language-evidence storage and read-only inspection; actual recognition remains an open operation 23 gate before translation in operation 25. The [language pipeline plan](language-pipeline.md) records the current build goal, dependencies, model comparisons, and validation criteria.

The user confirmed the existing multilingual survey, requested Radeon 780M acceleration where useful, authorized bounded model/corpus downloads and up to USD 20 cumulative external spend for OpenRouter validation, and chose automated reference-based and model-assisted evaluation without depending on new human reviewers on 2026-09-22. Keep required Canadian French, Navajo, and Klingon cases visible. LoRa and HackRF Pro hardware can be made available later; neither is needed for this milestone.

The user chose to keep Sigy on 2026-09-21; naming exploration is deferred. An operation is not a release. Local verification is `cargo verify` and `cargo verify-media` through `sigy-xtask`. Earlier notes that name `scripts/verify.ps1` describe runs completed before that change.

The public README now gives a bounded product preview, two labeled screenshots, source-install commands, a first-use path, update commands, and links to detailed guidance. The [install guide](../install.md) records prerequisites and platform limits. On Windows, both checked-in installer scripts passed syntax checks and isolated checkout-path invocation with a stub Cargo. A real `cargo install --path crates/sigy --locked --force` completed into an isolated root; that binary passed version/help, initialized a fresh library, ran `doctor`, and started, reported, and stopped its service. The GitHub one-liners, actual macOS/Linux execution, and OS startup are not qualified by those checks.

An independent installer review found and corrected fetch-status, checkout-path, source-identity, and Windows update-helper failures. The scripts now refuse a changed or different-origin managed tree before fetching, preserve a clean commit identity, and do not claim one for a dirty local checkout. `sigy update` prefers the running binary's embedded commit to an older global marker. A Windows PowerShell 5.1 and Git for Windows shell harness passed 16 local Git/stub-Cargo and synthetic wrapper cases, including relative and bracketed paths, refusal before Cargo, failed Cargo, and download status propagation. The Windows helper's missing-checkout regression test and `cargo verify` passed. These tests do not qualify a fresh Rustup bootstrap, actual macOS/Linux installs, or a tagged release.

## Work sequence

| Work | Current state | Evidence and remaining scope |
| --- | --- | --- |
| Foundation | Rust 1.98.1 and SQLite 3.53.4 selected | Initial Rust/Go probes and dependency review; native platform matrix incomplete |
| Catalog and financial ledger | Implemented with local tests | Exact amounts, atomic reservations, replay protection, uncertain liabilities, rollback and exclusive ownership; paid dispatch remains unavailable |
| Persistent controller and CLI | Implemented on Windows | Independent clients, detached/foreground operation, explicit stop, peer checks and bounded IPC; Unix and OS startup qualification pending |
| Capture journal and source authority | Implemented | Immutable source revisions, opt-in bounded redirects, exact worker generations, interrupted recovery, checked destinations at every hop and shared HTTP limits |
| Finite recording and retention | Implemented and locally tested | Decoder-backed publication, original-byte hashes, versioned metadata export, full byte reservations, 14-day/50 GB defaults, Keep/Archive, processing acknowledgment and staged deletion |
| Radio directory discovery | Implemented and locally tested | Bounded Radio Browser page refresh on a saved policy, offline multilingual search and favorites, visible observation age, immutable source provenance, interrupted recovery, and one live metadata smoke check. The cache is not a complete catalog |
| Optional network routing | Researched and planned | User-controlled proxy profiles and external VPN compatibility for personal machines or servers; DNS trust, credential handling and no direct fallback require qualification; no runtime proxy support |
| Direct listening | Operations 1 through 7 tested on Windows | Retained-file playback, one playlist document, an explicit directory click, one direct-revision listen, one finite HLS media playlist, explicit ICY observations, and one local format ladder on FFmpeg 9.0.1. A public station check remains unauthorized |
| Podcast discovery and listening | Operations 10 through 14 are in place | Subscribe performs no DNS and creates no capture. Refresh does not fetch transcripts or chapters. One enclosure can be downloaded and played |
| Continuous DVR and terminal experience | List explorer, segment playback, holds, and civil schedules are in place | Termina is the backend. The globe and visualizers remain at operations 36 and 37. Stage 4 stays open |
| Local analysis and monitoring | Supervised retained-input verification and legacy transcript inspection | Recognition remains open. Operation 24 evidence storage and bounded inspection are implemented. Measured recognition and aligned translation remain. Billing fixtures, the live queue, corrections, monitors, and briefings remain |
| Hardware and signal workbench | Planned after internet sources | Receive-only CB/AM/FM/shortwave and spectrum modes, typed IQ/packet observations, LoRa/Meshtastic and later decoders; actual device qualification required |

## Spending ledger

Cumulative authorized external-spend ceiling: **USD 20**, excluding coding-session costs. Settled spend: **USD 0**. Outstanding commitments: **USD 0**. Available: **USD 20**. The user raised the cumulative ceiling from USD 10 on 2026-09-22 and authorized paid OpenRouter validation within it; this is not an additional USD 20 allowance.

Pushing `main` or opening a pull request runs `cargo verify` on one standard GitHub-hosted Windows runner. [GitHub's billing documentation](https://docs.github.com/en/billing/concepts/product-billing/github-actions), reviewed 2026-09-22, states that standard hosted runners are free for public repositories. The public-source checkpoint is pushed only after visibility changes; no spending limit is raised. `cargo verify-media` stays local. No paid provider or recurring service is enabled. Use local builds, licensed fixtures, public documentation and free package retrieval first. Paid experiments require a mechanically bounded allocation and recorded reservation before dispatch, after the operation 27 billing-fault gate. Every model judge, retry, fallback and unresolved charge counts toward the same ceiling. The application's provider budget remains separately disabled by default. A product release waits until a roadmap stage exit is actually met.

| Date | Work or allocation | Reserved USD | Settled USD | State |
| --- | --- | --- | --- | --- |
| 2026-09-22 | Language pipeline research and local host inspection | 0 | 0 | Public documentation and local read-only checks; no inference |
| 2026-09-22 | Language-evidence storage, inspection, and local verification | 0 | 0 | Local fixtures and one free parser dependency; no inference or paid request |
| 2026-09-22 | Supervised input verification and public-source preparation | 0 | 0 | Local tests, dependency audit and credential review; no inference or paid request |
| 2026-09-22 | Native process boundary probe and CI maintenance | 0 | 0 | Local bounded child fixtures and action-pin review; no model, paid request, or hosted compute allocation |
| 2026-09-22 | Local network-boundary screen and ASR asset plan | 0 | 0 | One finite trusted-child loopback run and metadata-only model review; no model or paid request |
| 2026-09-22 | Offline language scorer and worker-boundary review | 0 | 0 | Synthetic scorer tests and source-level native worker study; no audio, model, or paid request |

The plan proposes an initial paid batch of at most USD 2 after billing qualification. It has no reservation yet. Record the concrete request manifest, exact maximum liability, runtime ledger reference, and later settlement here before executing a batch. The user raised the [new-download and experiment-workspace ceilings to 100 GB each](language-pipeline.md#confirmed-scope-and-working-limits) on 2026-09-22; acquisition still needs exact reservations and existing user models stay untouched.

## Verified evidence

The [supervised retained-input increment](../decisions/0034-retained-input-verification.md) passed `cargo verify` on Windows with 273 ordinary tests, warnings-denied Clippy, formatting, native-source hashes, build, and 304 dependency entries checked against 1,264 advisories. It adds 30 ordinary tests beyond the language-evidence checkpoint. `analysis verify`, `analysis job`, and `analysis cancel` use the existing service supervisor. A durable read lease prevents deletion while a worker reads; cancellation and supervisor abort retain ownership until actual blocking work returns. Restart interrupts without replay. New `analysis transcribe` requests fail without adding placeholders, while legacy rows remain readable. Schema is v25 and IPC is v26.

Fault tests exposed and corrected two concrete defects: terminal catalog failure could leave shutdown waiting on an already-finished worker, and deleting the final retained segment could remove the file before a zero-byte accounting update failed. The first now stops admission while retaining the durable lease, and permits shutdown to drain; the second uses staged whole-recording deletion. Tests preserve SQL constraints and verify physical files, exact charge, partial segment release, final deletion, restart, and reopen integrity. The 60-second file-read deadline is cooperative, not a hard bound on a hung filesystem call. Native inference containment and language quality remain open.

`cargo verify-media` passed all 12 native-media tests on Windows with the installed FFmpeg 9.0.1. The retained-recording fixture now also runs pin admission/publication, asynchronous verification, job inspection, exact replay, and transcription refusal through the CLI and service. The retained bytes match the verification receipt and no transcript is created. No public station, model, or paid provider was contacted by those fixtures.

The [public-source review](public-readiness.md) records publication preparation and its limits. GitHub visibility and crates.io distribution are separate decisions. The user requested GitHub first and explicitly deferred crates.io publication on 2026-09-22. Public availability is not a roadmap stage exit or a supported release.

The repository became public on 2026-09-22 at checkpoint `8aa072d`. Secret scanning, push protection, and private vulnerability reporting are enabled and verified by API readback. Anonymous clone and installer-byte checks passed. The [first public Windows run](https://github.com/blisspixel/sigy/actions/runs/35796307780) passed `cargo verify`. Crates.io publication remains deferred. Native process qualification continues from the candidate evidence and unresolved isolation limits in [the research record](../../research/30-language-pipeline-evaluation.md#windows-boundary-candidate-2026-09-22).

The [native process probe](../../research/experiments/native-boundary/README.md) now exercises ProcessKit 3.3.4 in a standalone, workspace-excluded package. Windows Job Object checks refused an invalid cap, a second child, and a bounded allocation, then reached zero active members after terminating a grandchild and an output flood. A single CPU comparison showed less CPU time under a quota. The observed peak job commit exceeded the configured 32 MiB by 876,544 bytes, so the configured value is not yet an exact observed-memory ceiling. The candidate is not integrated into Sigy. Network denial, the spawn-assignment gap, abrupt owner death, and remaining fault probes are open before model execution. The [first hosted run with the new action pins](https://github.com/blisspixel/sigy/actions/runs/35799240617) passed `cargo verify`.

The [Windows network-boundary review](../../research/30-language-pipeline-evaluation.md#windows-network-boundary-research-2026-09-22) led to six disposable AppContainer probes. Ordinary IPv4 and IPv6 loopback controls passed. The second captured original `CreateProcessW` HRESULT `0x80070057` (invalid parameter). A one-expression, ignored dependency experiment changed the zero-capability pointer to null; the third then captured `0x800700CB` (missing environment option). Adding only validated `LOCALAPPDATA` let the fourth trusted child launch. The fifth verified its package token report and observed no successful IPv4 or IPv6 loopback TCP connection: both attempts timed out, and neither listener saw a contained connection. The sixth repeated that TCP result and tested four finite local datagram exchanges. Pre/post controls delivered all four; the contained child reported four sends then reply timeouts, while the observer saw no contained datagram. A timeout is not an OS permission-denied code or general egress proof. DNS-wire packets did not exercise Windows DNS Client. All six exact task-owned profiles were deleted through the API, their staging directories removed, and no child process remained. The sixth run also checked current-user AppContainer mapping display names and descriptions after cleanup. Both mutation paths are disabled again. The local patched wrapper is not selected for Sigy. Broader network isolation, actual resolver behavior, job assignment at creation and resource control remain open before model execution. The existing ProcessKit result is resource and descendant evidence only.

The same review found no drop-in safe Rust worker boundary with both job membership at creation and aggregate memory, process-count, and CPU limits. A trusted-broker composition is a proposed proof, not a selected runtime; it retains the broker's own creation window. The [latest public Windows run](https://github.com/blisspixel/sigy/actions/runs/35805100104) passed `cargo verify` for the UDP research and pinned CPU asset proposal.

The [frozen eight-language FLEURS screening selection](../../research/experiments/language-corpus/README.md) identifies 112 train/test clips, 90.42% of selected duration non-English, with disjoint parallel sentence groups. It is read-speech smoke material, not a support claim. Its full archives total 15.01 GiB. Strict immutable Parquet footer ranges worked, but the needed audio column chunks total 16.02 GiB. Both exceeded the initial 10 GiB new-download allowance; the new 100 GB ceiling removes that size obstacle, but acquisition provenance and accounting remain open. The eight test archives alone total 3.19 GB but omit train calibration; that partial route has not been adopted. No audio or paid request was made. The [public Windows run](https://github.com/blisspixel/sigy/actions/runs/35801903476) for the corpus-evidence commit passed `cargo verify`.

One pinned French test sentence returned two Dataset Viewer rows. The selected filename and reference hash matched, and its signed asset path contained the pinned source revision; HEAD reported 518,458 bytes. No audio was fetched. The remaining 111 clips and a cumulative transfer bound are unverified, so the acquisition gate stays closed.

The [corpus acquisition route decision](../../research/experiments/language-corpus/acquisition-route-decision.md) now selects all 16 revision-pinned original archives, 16,113,568,959 declared bytes, for stronger whole-object hash verification under the raised ceiling. The temporary per-clip route is not selected. A full former-cap charge of 10,737,418,240 bytes covers unmeasured experiment traffic before the 2026-09-23 02:47:49 UTC cutoff; it is a policy charge, not measured consumption. A HEAD-only pilot on the smallest archive matched the publisher length across one checked public redirect, with no audio body. One-ledger transfer, whole-archive hashes, bounded extraction and a single-archive GET pilot remain before admitting the full set. No audio GET or model download has run.

The user requires three audio files to pass end to end with timing and resource evidence before expanding to ten, then to the frozen 112. Independent providers are not required for this first subset. The first archive GET will be one step inside that three-file pilot; a passing transfer or decode alone cannot open the next stage. Frozen holdout references remain reserved for later evaluation.

The [three-clip plan](../../research/experiments/language-corpus/three-clip-pilot.json) names Arabic, Hindi and Spanish train clips from distinct archives and sentence groups, totaling 34.62 seconds of speech and 3,810,213,582 declared archive bytes. Its source identities and totals match the frozen metadata. Audio acquisition and model execution remain disabled; the plan has produced no transfer or recognition result.

The [pinned CPU ASR asset proposal](../../research/experiments/local-asr/README.md) identifies two publisher-hashed objects totaling 496,175,237 bytes. Its acquisition manifest is disabled. No model or runtime archive has been downloaded or executed. The reviewed release has no Windows Vulkan package, so the iGPU comparison still needs a separate build and proof of device use. The [public Windows run](https://github.com/blisspixel/sigy/actions/runs/35803485168) for the fifth AppContainer research record passed `cargo verify`.

The [offline language scorer](../../research/experiments/language-scorer/README.md) passed 24 synthetic tests, formatting, warnings-denied Clippy, build, a cached advisory check, and 32/80 metadata selection checks on Windows. It has no real ASR results. Its [public Windows run](https://github.com/blisspixel/sigy/actions/runs/35807732660) passed `cargo verify`. The standalone asset downloader and compile-only broker proof remain disabled in ignored `.agents/`. An independent source review found the downloader's earlier unchecked DNS answers and blocking lookup addressed; 28 offline tests, formatting, warnings-denied Clippy and build passed. Actual DNS, TLS, cancellation packet timing and filesystem durability remain unqualified. These are research tools, not selected product components.

The preceding redirect checkpoint is commit `5011e1d`. The favorites increment passes **92 ordinary tests plus 4 explicit native-media tests** on Windows x86_64, with the intermittent media-fixture caveat below. Retained-file playback adds two device-list unit tests and a fifth native-media test: null-destination progress and seek, unchanged quota, a killed client, and a system-destination failure on the installed FFmpeg 9.0.1 build, which has no local audio output device. Playlist resolution adds parser, catalog, and one service fixture: a local playlist is resolved without an audio connection, accept leaves the parent revision unchanged, replay does not fetch or register again, an HLS document stores no entries, and a directory HLS flag fails before a fetch. The audio acquisition path still rejects playlist media types. `./scripts/verify.ps1` passed native source hashes, formatting, workspace tests, warnings-denied Clippy, build and dependency auditing. `./scripts/verify-media.ps1` passed the six native-media tests on the installed FFmpeg 9.0.1 executable, including direct-listen playback. The advisory check reviewed 240 dependency entries against 1,258 loaded advisories without findings. Playlist resolution and direct listen add no application dependencies. The lockfile lists the separate `sigy-xtask` workspace member. Local fixtures show one direct WAV reaching decoder progress on `--destination null`, an unchanged quota, no retained audio file, and no second connection on replay. A playlist content type and an `icy-metaint` response fail before decode. Service kill during a held connection leaves no playable file and no reservation; restart marks that listen interrupted and replay does not reconnect. Stopping a listen leaves another revision's recording in progress.

Operation 5 passes local tests. `record hls` fetches one finite media playlist on the shared acquirer and publishes the concatenated segments through the existing recording path. Parser tests reject a missing `#EXTM3U`, a live playlist before segment authorization, encryption, a map, a byte range, discontinuity, partial segments, a tag split by a multibyte character, an empty ended playlist, a cross-origin segment, and a 33rd segment. Allowed playlist tags remain one segment. The ignored service fixture serves two WAV halves, publishes a file equal to their concatenation, and records a failed master playlist with no request path containing `variant`. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 240 crate dependencies against 1,258 advisories. No application dependency was added. `cargo verify-media` passed the seven native-media tests, including that HLS fixture. Catalog schema remains v10. Local IPC is v11. During the first full verification in this change, `explicit_click_is_one_request_and_discards_the_stream_url` saw directory refresh state `failed` instead of `completed`. That assertion did not yet include the refresh record, so the failure text was not captured. The acceptance condition and deadline are unchanged, and the assertion now includes the record. Three later service-suite runs and `cargo verify` passed. The same refresh later failed on the Windows CI runner with `connection or response headers`. The fixture was accepting on a nonblocking listener and then reading the request immediately, so a client that had not sent bytes yet reset the connection. Accepted sockets are now placed in blocking mode before that read. The acceptance condition is unchanged.

New favorites tests preserve saved choices through changed metadata, omitted stations and reopen; enforce cache-row protection; combine filters before pagination; and check failed writes and v6 migration rollback. The real CLI fixture saves and removes preferences with the service running and stopped, checks restart persistence and inspects the plain-text favorite marker. No stream or additional directory request is sent by those operations.

`./scripts/verify-media.ps1` passed using the installed FFmpeg 9.0.1 executable. Real CLI processes record a bounded local WAV fixture, preserve its exact bytes, validate positive decoded duration, export metadata, reconcile replayed requests, protect kept media, acknowledge processing and reclaim temporary media. Other fixtures reject non-audio bytes with an audio MIME label and kill the service after partial reception. Restart retains partial bytes and their reservation, marks the attempt interrupted, and does not issue another source request on replay.

The new media fixture follows an explicitly permitted same-origin redirect and publishes exact audio bytes with envelope v2 route observations. Transport tests cover all supported redirect statuses, no cookie/auth/referer forwarding, malformed/duplicate Location fields, denied secondary connections, loops, hop and byte caps, shared deadlines and stop during headers. Policy cases cover public/private destinations and downgrade. Migration and storage tests preserve v5 permissions, reject conflicting policy replay, roll back failed migration and reject invalid or rewritten publication provenance.

HTTP tests distinguish intentional duration limits and user stops from failed transport and empty recordings. A regression test caught and fixed the interaction between request read timeouts and planned recording completion. Migration tests cover v3-to-v4 success, conflict rollback and preservation of existing financial limits. Retention tests cover age, quota pressure, processing acknowledgments, active/protected media, competing reservations, stale publication and deletion accounting.

Deleting interrupted media closes its pending intent while retaining history and replay protection. The CLI exposes `record`, `dvr` and `radio`. Directory fixtures exercise real client/service requests, Unicode filters, exact replay, immutable source registration without stream contact, oversized replies and process-kill recovery. Storage tests cover atomic page rollback, invalid rows, duplicate identities and migration preserving financial and DVR policy. Documentation checks cover local links/anchors, C-01 through C-41, R-01 through R-58, writing rules and diff whitespace.

A live directory smoke check on September 21 exercised automatic SRV mirror discovery, bounded HTTPS metadata acquisition, durable publication and cached CLI search. The corrected `--name france --limit 5` request accepted five station records with no rejected rows. An earlier request exposed the effect of empty API filters; unused parameters are now omitted and a fixture checks the request shape. This validates one directory interaction, not complete-world coverage or audio compatibility.

A prior public-radio audio smoke attempt was blocked before execution by automatic approval review; no public audio recording was obtained. Local media fixtures do not establish broad codec qualification. Publisher-feed research fetched only bounded RSS metadata, without episode media or secondary assets. No inference, physical radio or paid service was exercised.

The available host is Windows x86_64, AMD Ryzen 7 7840U, approximately 64 GiB RAM. A reported host crash has no established cause. Builds remain limited to two jobs, ordinary tests to two threads per binary and native-media tests to one. Test subprocesses and fixture servers have finite deadlines. Other operating systems, small-host capacity and language quality remain unqualified.

During redirect verification, an existing directory crash fixture once timed out waiting five seconds for its local request. The original cause was not captured. Failure diagnostics now include the durable refresh status, with the deadline unchanged. The targeted test, three complete service-suite repeats and the final full verification passed. Investigate with those diagnostics if it recurs; this is not evidence of qualified long-running reliability.

During favorites verification, the existing malformed-audio media fixture once reached the expected failed/reserved state but did not report the expected decode failure detail. The original detail was not printed, so its cause is unresolved. The assertion now includes the recording state on failure; its acceptance condition and deadlines are unchanged. The diagnostic rerun and three additional complete native-media runs passed. This remains a tracked intermittent verification issue, not a repaired runtime defect or a clean reliability claim.

## Current contracts and limits

Catalog schema is v25 and local IPC is v26. The README introduces the product and shows `sigy --help` plus an empty list-explorer frame from this Windows host. Command detail is in the [usage guide](../usage.md). `sigy doctor` reports catalog integrity, cache age, decoder, quota, and interrupted captures without a network request. Stations older than 24 hours stay cached. A station row with no observation time stays cached and is reported stale. `cargo verify` for that catalog check passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. No application dependency was added. `cargo verify-media` was not rerun. `scripts/install.sh` and `scripts/install.ps1` install the `sigy` binary. The README one-liners read those scripts from GitHub. A logged-in GitHub CLI can supply Git credentials. `sigy update` installs the latest `main` commit, and `sigy update --check` only reports it. They do not install an operating-system service or download FFmpeg, and they are not `cargo verify`. The list explorer and human command output use the terminal palette for connection, health, favorites, and failures. `NO_COLOR`, `--monochrome`, `--linear`, `TERM=dumb`, and a pipe leave the words uncolored. Recording exports are envelope v2, or v3 when ICY observations exist. Stop older controllers before replacing binaries. Existing schema versions migrate transactionally; automatic backups and power-loss recovery still require qualification. The single library owner retains the lock through workers and catalog operations. Read [controller](../decisions/0002-local-controller.md), [journal](../decisions/0003-capture-journal.md), [source authority](../decisions/0004-source-authority-and-http.md), [recording/retention](../decisions/0005-recording-and-retention.md), [directory refresh](../decisions/0006-radio-discovery.md), [authorized redirects](../decisions/0007-authorized-redirects.md), [favorites](../decisions/0008-radio-favorites.md), [playlist resolution](../decisions/0009-playlist-resolution.md), [directory clicks](../decisions/0010-directory-clicks.md), [direct listen](../decisions/0011-direct-listen.md), [HLS media playlists](../decisions/0012-hls-media-playlist.md), [ICY observations](../decisions/0013-icy-observations.md), [decoded formats](../decisions/0014-decoded-formats.md), [terminal stack](../decisions/0015-terminal-stack.md), [list explorer](../decisions/0016-list-explorer.md), [local podcast subscriptions](../decisions/0017-local-podcast-subscriptions.md), [RSS feed refresh](../decisions/0018-rss-feed-refresh.md), [episode enclosure download](../decisions/0019-episode-enclosure.md), [retained episode playback](../decisions/0020-retained-episode-playback.md), [agent plugin](../decisions/0021-agent-plugin.md), [publisher text](../decisions/0022-publisher-text.md), and [measured recording intervals](../decisions/0023-recording-intervals.md).

Directory refresh merges one requested page, at most 500 rows and 2 MiB, into a cache of at most 10,000 stations. It does not delete unseen stations or claim complete coverage. Refresh requires the service; search/show/registration can use the local cache offline. One active refresh, finite history, deadlines and shared network slots bound work. A saved policy refreshes one bounded page on the service clock. It is not a complete catalog and not a second provider. Opening a client does not run it. Directory metadata, health and languages are not detected content or verified stream compatibility.

The current recording profile accepts HTTP audio with an explicit deny/same-origin/public redirect policy and a three-hop ceiling. Existing sources migrate with redirects denied. Checks, concurrency and total deadlines apply across the route; pins cannot grant cross-origin access and HTTPS cannot downgrade. Playlist documents can be resolved separately: 64 KiB, the directory document deadline, at most 32 entries, and no nested fetch. The direct audio path still rejects playlist media types. HLS markers and a directory HLS flag fail resolution and store no candidates. `record hls` is the separate finite media-playlist recording path. `record start --icy` splits interleaved metadata out of the audio. Listen, HLS, and the default recording still reject it. Automatic retries and proxies remain unsupported. Two active attempts, 15 minutes and 256 MiB per radio attempt are conservative bounds, not measured capacity claims. An episode download reserves 512 MiB and 30 minutes and does not raise that radio ceiling. A retained episode plays with `listen file` and has no live edge. Hickory provides asynchronous DNS with checked destinations; TLS retains offline verification and explicit roots. The external decoder receives local bytes only and is not bundled or covered by the Rust advisory audit. On Windows x86_64 with FFmpeg 9.0.1, one local ladder decoded direct WAV, MP3, AAC (ADTS served as audio/aac), FLAC, and Ogg Vorbis, and WAV from one accepted playlist entry behind one same-origin redirect. See [decoded formats](../decisions/0014-decoded-formats.md).

Envelope v2 exports redirect policy and optional origin/peer/status observations published with successful media. Paths and queries are excluded, legacy/unavailable evidence is null, and failed-attempt routes are not retained yet. These observations are provenance, not new permissions.

Media quota includes retained bytes and full outstanding reservations. Kept/archived objects remain protected and count toward quota. Failed/interrupted bytes remain unverified and charged until deletion. Archive is not backup. Explicit processing acknowledgment only records a receipt; it does not run analysis. The service sweep deletes that temporary recording. Kept and archived recordings stay. Metadata exports are snapshots and do not confer authority. Original checksums are recorded at publication; external file-change reconciliation remains open.

The free-space floor cannot reserve space against other programs. Catalog/WAL, models, exports and future derivative caches need separate storage policies. Native aggregate memory/CPU sandboxing, decoder fault corpora, Unix parent-death behavior, filesystem ACL qualification, physical power loss, migration backups and source-permission revocation remain release gates. No OS startup service, release, platform certification, globe, or provider dispatch exists. The list explorer is the optional client in operation 9. Local podcast subscribe is operation 10. RSS refresh is operation 11. It lists episodes and does not download an enclosure or start a capture. Operation 12 downloads one explicit enclosure. Operation 13 plays that retained file with `listen file`. The episode has no live edge. Subscribe and refresh still do not download or play.

## Next bounded work

The active build goal is the [retained-recording language pipeline](language-pipeline.md). Operation 24's [bounded evidence contract](../decisions/0033-language-evidence.md) preserves empty v23 transcripts as unmeasured history. Supervised retained-input verification and retention coordination are implemented. Next are enforceable native resource bounds, bounded decoding, and a measured recognizer to complete operation 23. Real output, truthful language evidence, and aligned English translation must pass the frozen evaluation before any language-quality claim. Provider configuration and billing faults precede the authorized remote comparison. The storage increment advances catalog schema to v24 and local IPC to v25.

The 2026-09-22 review corrected documentation and prepared the [dated research](../../research/30-language-pipeline-evaluation.md). Read-only host inspection found Radeon 780M driver `32.0.31007.5012` and Ollama `0.34.2`. This does not demonstrate GPU offload or model compatibility. No model was downloaded or run and no paid call was made during planning. Model selection and measured quality remain open.

Documentation validation checked 16 changed Markdown files and 271 local links and anchors, with no broken targets or prohibited dash characters. `git diff --check` passed. Runtime source, dependencies, schemas, and protocol versions did not change in this planning increment, so `cargo verify` and `cargo verify-media` were not rerun. The build goal remains active; documentation completion is not recognition or translation completion.

The subsequent operation 24 increment implements [language-evidence storage and inspection](../decisions/0033-language-evidence.md). It preserves exact input/transcript lineage, provider labels, mixed and overlapping observations, separate outcome and route capability, and immutable revision replay. Bounded pages are exposed by `analysis languages list` and `analysis languages show`. Empty legacy cues create no observations. A coarse detector block can be smaller than a recording segment, and a span-free unavailable-input outcome remains recordable after retention expiry. No detector or model is run.

`cargo verify` passed native-source hashes, formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 304 crate dependencies against 1,264 advisories on this Windows host. New checks cover semantic distinctions, tag grammar and normalization, script evidence, media/gap bounds, empty transcript rejection, stale revisions, expiry and replay, immutable history, capacity, rollback, malformed stored payloads, v23 migration preservation/failure, bounded pages, command parsing, and terminal controls. The first full run found that the legacy v15 migration fixture omitted the newly introduced table when constructing its old schema. Its setup now removes that table before its parents; the acceptance assertions are unchanged, and the full rerun passed. CLI help was inspected. `cargo verify-media` was not rerun because acquisition, decoding, recording, and playback behavior did not change. Schema is v24 and IPC is v25. `oxilangtag` 0.1.6 is the only new crate; its active feature tree has no dependencies. No models, corpus assets, inference, or paid requests were introduced.

The successful operation 24 verification ran 243 tests, including 17 added language checks. The ordinary command left 12 native-media tests ignored. Documentation validation checked 18 changed Markdown files and 311 local links/anchors; `git diff --check` also passed. These are local storage and interface results, not a language-quality benchmark.

The existing publication, capture, playback, retention, and authority contracts remain in force. A public station check waits for separate authorization. The full first-release order remains the [build order](../../ROADMAP.md#build-order); hardware follows internet sources and the first release.

Operation 1 is in place. `listen file` plays one retained recording in the client through the configured FFmpeg decoder, with a file protocol whitelist and no source URL. The tested path is `--destination null`, including a seek inside the published duration, unchanged quota, and a killed client leaving the recording retained. `--destination system` fails when that FFmpeg build has no local audio output device. The installed FFmpeg 9.0.1 build on this Windows host has no such device, and the media test observes that failure.

Operation 2 is in place. `source playlist resolve` reads one playlist on the shared acquirer: 64 KiB, the directory document deadline, the revision's redirect policy, at most 32 entries, and no nested fetch. An HLS marker or a directory HLS flag fails the resolve and stores no candidates. `source playlist accept` registers a new immutable audio revision for one chosen index and performs no network I/O. Replay does not fetch or register again. The parent revision is unchanged. Ordinary views omit entry paths and queries. Schema and IPC are v8. The local service fixture covers a playlist document, no audio connection during resolve or accept, the unchanged parent, HLS rejection, and the directory flag. The audio acquisition tests still reject playlist media types. Podcasts, the TUI, and analysis stay unimplemented.

Operation 3 is in place. `radio click` sends one `GET /json/url/{stationuuid}` on the shared acquirer, with redirects denied. The returned stream URL is not stored or followed. Search, show, favorite, and refresh do not send it. Replay does not send it again. A second click ID inside two seconds is refused before a request. The local service fixture covers one counter request, no vote path, and no `secret-stream` in the status. Schema and IPC are v9.

Operation 4 is in place. `listen source` reads one direct audio revision on the shared acquirer, at the existing 15-minute and 256 MiB limits, and writes a private local pipe. The client decodes that pipe with the pipe protocol whitelist. The tested destination is `--destination null`. The service does not open an audio device. A playlist content type and an `icy-metaint` header fail before decode. Replay does not open the source again. Service kill leaves no playable file and does not change charged or reserved bytes. Restart marks the running listen interrupted, and reusing that ID does not reconnect. Stopping a listen does not stop a recording of another revision. The same revision cannot be listened to while a recording holds it. Schema and IPC are v10.

Operation 5 is in place. `record hls` reads one finite media playlist on the shared acquirer. The document must contain `#EXTM3U` and `#EXT-X-ENDLIST`, and it allows at most 32 segments. Segment bytes and time share the recording ceiling. The defaults are 60 seconds and 64 MiB. A master playlist fails before a variant URL is authorized or fetched. A live playlist fails before segment authorization. Encryption, a map, a byte range, discontinuity, partial segments, and any other tag are rejected. A cross-origin segment is denied under the parent policy. The service publishes one local file through the existing decoder check, hash, and quota. The local fixture's published bytes equal the two WAV halves, and the master request does not fetch a path containing `variant`. Segments do not become source revisions. The decoder does not receive an `.m3u8` address. Catalog schema remains v10. Local IPC is v11. There is no migration.

Operation 6 is in place. `record start --icy` asks for interleaved metadata and publishes only the audio bytes. The default sends `icy-metadata: 0` and fails before a body write when `icy-metaint` is present. Listen and HLS stay on that default. The local fixture's published WAV equals the audio with the title removed, and its SHA-256 matches those audio bytes. The export is schema version 3, contains one hostile title and stream URL, leaves the source name unchanged, and the fixture records no request for that URL. Replay must use the same metadata flag. Ordinary tests cover that audio-only hash, rejection of an observation when metadata was not requested, and v10 migration rollback. Catalog schema is v11. Local IPC is v12. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 240 crate dependencies against 1,258 advisories. No application dependency was added. `cargo verify-media` passed the eight native-media tests, including this fixture. Non-UTF-8 metadata and public stations are not qualified.

Operation 7 is in place. One ignored service fixture generates WAV, MP3, AAC (ADTS), FLAC, and Ogg Vorbis with the configured FFmpeg and serves them on 127.0.0.1. Direct recording requires publication formats wav, mp3, aac, flac, and ogg, each with a positive decoded duration. The same server answers `/list` with one same-origin redirect to `/final.m3u`, whose one relative entry is the WAV clip. Accepting that entry, recording it, and `listen source --destination null` complete as WAV with advancing progress and a positive playhead. Recorded request paths stay inside that ladder. The README names those formats for Windows with FFmpeg 9.0.1. One smoke session is not a support matrix. A public station check waits for separate authorization. Catalog schema stays v11. Local IPC stays v12. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 240 crate dependencies against 1,258 advisories. `cargo verify-media` passed the nine native-media tests, including this fixture.

Operation 8 is in place as a measurement, not a client. The excluded probe at `research/experiments/terminal` ran one list and search workload on ConPTY and in Windows Terminal 1.24.11911.0 at 80x24, a requested 120x36, and a requested 200x50. Ratatui 0.30.2 with Termina 0.3.3 kept idle redraws at zero, restored console modes after exit and panic, kept a combining mark in one cell and `東京` on wide cells, honored `NO_COLOR` and reduced motion, and treated a bracketed paste containing `q` as text. Crossterm 0.29.0 matched those checks except paste. On ConPTY the driver timed out and the report has no paste event. Inside Windows Terminal the query became `navajo[20~` and the report has no paste event. Release key-to-frame samples for Termina were 123 to 315 microseconds on ConPTY and 76 to 1071 microseconds inside Windows Terminal. That is event-read to draw flush, 18 keys each, not glass-to-glass. Windows Terminal clamped the larger requests to 115x36 and 115x37. The console font face was Consolas. No application dependency was added. The client still opens no second catalog. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 240 crate dependencies against 1,258 advisories. See [terminal stack](../decisions/0015-terminal-stack.md).

Operation 9 is in place. `sigy tui` renders the list explorer on Ratatui 0.30.2 with the Termina 0.3.3 backend and does not enable Crossterm. The screen shows the service connection or local catalog, partial cache, observation age, refresh outcome, search, favorites, selected source, directory health, directory languages, recordings, quota, and playback. Directory health is not playback and not detected language. Selection does not start audio, capture, refresh, or a click. Quit does not stop the service or a recording. Reduced motion, linear order, and monochrome exist, and the client draws no animation. Monitors, Findings, the globe, and the map stay visibly unavailable. State tests cover focus, stale search and favorite responses, disconnect, and quit during a running recording. The 80x24 layout keeps search, identity, playback, and help. The Windows inspection on this host ran inside Windows Terminal 1.24.11911.0 at 80x24 with the Consolas font, `WT_SESSION` set, and an empty local catalog that did not start audio. The client opens the catalog only when the service does not hold it, and otherwise uses the existing IPC path. Catalog schema stays v11. Local IPC stays v12. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 281 crate dependencies against 1,258 advisories. This change does not touch media acquisition or the decoder, so `cargo verify-media` was not required. See [list explorer](../decisions/0016-list-explorer.md).

Operation 10 is in place. `podcast subscribe` stores one feed subscription. The feed URL, network scope, address pin, and redirect policy are immutable. Unsubscribe sets polls to stopped, keeps the row, and deletes nothing. Subscribe does not resolve DNS, register an `http_audio` revision, or create a capture. A second active subscription for the same normalized feed URL is rejected. Subscribe does not reactivate a stopped id. Ordinary views omit the feed path and query. The list explorer does not subscribe. Catalog schema is v12. Local IPC is v13. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 281 crate dependencies against 1,258 advisories. This change does not touch media acquisition or the decoder, so `cargo verify-media` was not required. See [local podcast subscriptions](../decisions/0017-local-podcast-subscriptions.md).

Operation 11 is in place. `podcast refresh` reads one RSS 2.0 document on the shared acquirer. The compressed body is at most 2 MiB, the decoded document is at most 8 MiB, and the expansion ratio is at most 16. At most 500 items are committed. Further items mark the snapshot truncated. Episode identity prefers the publisher guid. A missing guid may be derived from a normalized enclosure URL plus a publication time, and that derivation is labeled. Titles are not identity. Omission from a later snapshot does not delete an episode. A failed document, including a DTD, leaves the last good snapshot. Transcript and chapter URLs are stored and not fetched. A live item is counted and not opened. A private redirect fails closed. The local fixture lists 239 episodes offline, including one derived episode, and the fixture contacts only 127.0.0.1. Replay does not fetch again. Subscribe still performs no DNS and creates no capture. The list explorer does not refresh or list episodes. Catalog schema is v13. Local IPC is v14. flate2 1.1.10 was added for bounded gzip and deflate. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the nine native-media tests. See [RSS feed refresh](../decisions/0018-rss-feed-refresh.md).

Operation 12 is in place. `podcast download` fetches one stored enclosure on the shared acquirer and publishes it through the existing recording path. The revision uses the subscription network scope, address pin, and redirect policy. The source name is Episode, not the item title. Before connect, the service reserves 512 MiB and 30 minutes. Radio attempts remain 15 minutes and 256 MiB. An RSS length above 512 MiB is rejected before connect and creates no source or recording. An HTTP Content-Length above the ceiling writes no body bytes. A byte-limit end is not playable. Publication requires a clean end, decode, and SHA-256. Replay of the same recording id does not download again, and a second id is refused while that recording is reserved or retained. The local WAV fixture contacts only 127.0.0.1. Its published bytes match the fixture. `record list`, `record path`, `record keep`, and `dvr status` see it. Transcript and chapter URLs are not fetched. Subscribe and refresh still do not download. Catalog schema is v14 and local IPC is v15. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the ten native-media tests, including this fixture. See [episode enclosure download](../decisions/0019-episode-enclosure.md).

Operation 13 is in place. `listen file` plays one retained episode through the existing file player. The episode has no live edge. `listen source` on that enclosure revision fails before a listen receipt and before any request. Subscribe and refresh still start neither download nor playback. The local WAV fixture contacts only 127.0.0.1. It subscribes, inspects the subscription and episode list, downloads one enclosure, and plays the retained file to `--destination null`. The policy is the default 14 days and 50 GB. The recording stays temporary. Playback advances inside the published duration and does not change charged or reserved bytes. A seek at the published duration fails. A later refresh does not fetch the enclosure or a transcript. Catalog schema stays v14 and local IPC stays v15. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the eleven native-media tests, including this fixture. See [retained episode playback](../decisions/0020-retained-episode-playback.md).

Operation 14 is in place. `podcast text` fetches one stored transcript or chapter document on the shared acquirer. Refresh does not fetch that URL. Accepted documents are WebVTT, SRT, podcast JSON transcripts, and podcast JSON chapters. HTML is rejected before connect. Nested chapter image and link URLs are not requested. The record stores the final origin, response type, feed language hint, document SHA-256, alignment `unverified`, and attribution `publisher`. Cue times are `publisher_start_ms`. They are not media time, and the text is not an ASR row. The bytes do not change charged or reserved quota. Replay of the same id does not fetch again. One fetch may run. Service restart marks a running fetch interrupted and leaves the previous snapshot. A chapter index above 3 is rejected. A local fixture contacts only 127.0.0.1. Catalog schema is v15. Local IPC is v16. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the eleven native-media tests. The publisher-text fixture is an ordinary test. See [publisher text](../decisions/0022-publisher-text.md).

Operation 15 is in place. A published recording stores one interval whose bounds are the decoded duration and the published byte length, both starting at zero. The requested `duration_seconds` stays the planned window and is not copied into the interval. A reserved recording has no interval. Opening a schema v15 catalog projects the published rows and leaves unpublished rows alone. The interval is written in the publication transaction, then kept when the file is deleted. Catalog schema is v16. Local IPC stays v16. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the eleven native-media tests. See [measured recording intervals](../decisions/0023-recording-intervals.md).

Operation 16 is in place. A radio capture whose byte budget is at least 32 MiB seals while the job stays running. The open segment ceiling is 32 MiB. The seal closes on that ceiling or on 5000 ms of receive time, whichever comes first. That 5000 ms bound is the candidate uncommitted window, not a measured durability result. Admit escrows the whole byte budget. Opening a segment assigns one ceiling. Sealing charges actual bytes and returns the unused difference to the escrow. The next segment opens only when a full ceiling remains. Renewal extends the lease on the same socket and does not open a second request. A stale token cannot seal. The local fixture publishes two ordered WAV intervals on one socket, keeps the job running across the seals, and shows one open reservation. A capture below 32 MiB, HLS, and an episode stay one file. `listen file` does not play a multi-segment timeline. Catalog schema is v17. Local IPC is v17. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the twelve native-media tests. See [segment seals](../decisions/0025-segment-seals.md).

Operation 17 is in place. Disconnect, service recovery, a codec change, a refused segment renewal, `record pause`, and a backward clock each write one gap for the planned window that published audio does not cover. The gap stores the cause and the microsecond range. It has no object, hash, or audio file. A codec change does not append the new format. An expired lease is not extended. A backward clock is not stored as the capture time. `listen file` refuses a seek inside a gap before the decoder starts. A seek in published audio is unchanged. Catalog schema is v18. Local IPC is v18. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` was not rerun. See [capture gaps](../decisions/0026-capture-gaps.md).

Operation 20 is in place. `schedule create` stores one rule for one source revision, one IANA zone, and a once, daily, or weekly civil clock. Each occurrence has a finite duration and a finite byte budget inside the radio ceiling. Only the next occurrence is materialized. Replay and restart do not admit a second job. A window that has already ended stays missed and is not backfilled. A spring-forward civil time is missed. A fall-back civil time admits one occurrence on the earlier offset. A late start records a `late_start` prefix gap and keeps the original plan. Changing the rule does not rewrite that admitted plan. No analysis profile can be attached. The service checks about once a second and admits the open occurrence when a decoder file is configured. Civil time uses the IANA data bundled with jiff 0.2.37. Catalog schema is v20. Local IPC is v21. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 303 crate dependencies against 1,261 advisories. `cargo verify-media` passed the twelve native-media tests. The publication path places a following interval after a prefix gap, and a recording without that gap is unchanged. See [recording schedules](../decisions/0029-recording-schedules.md).

Operation 21 is in place. `radio policy set` stores one bounded directory page and an interval of 1 to 168 hours. The service admits the current slot after that interval. A missed slot is not backfilled. A completed, failed, or interrupted slot is not fetched again. One refresh runs at a time. Opening a client does not fetch a page, probe a station stream, or send a click. A failed refresh leaves the last usable cache. Favorites stay. Catalog schema is v21. Local IPC is v22. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 303 crate dependencies against 1,261 advisories. `cargo verify-media` was not rerun because the decoder and acquisition path are unchanged. See [directory refresh policy](../decisions/0030-directory-refresh-policy.md).

Operation 22 is in place. `analysis admit` pins one completed recording. The pin stores the retained checksum and the media clock. A capture gap stays a gap, and planned time with no audio is an uncovered gap. `record processed` does not create the pin. An unpublished recording is refused. A checksum that does not match the published interval is refused. Replacing an unpublished worker leaves the old revision unable to publish. The pin has no source URL and does not reserve a paid budget. Catalog schema is v22. Local IPC is v23. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 303 crate dependencies against 1,261 advisories. `cargo verify-media` was not rerun because the decoder and acquisition path are unchanged. See [analysis inputs](../decisions/0031-analysis-inputs.md).

Historical operation 23 persistence slice, superseded for new work by decision 0034: its recognition gate remains open. `analysis transcribe` reads one published pin and hashes the retained local files. It does not open a source URL or decode speech. No measured recognizer is selected. The stored revision is original script. Each published interval has one cue with an empty script and wording `uncertain`. A gap has no cue. Replay returns that revision. A rollback before commit leaves no row. The decision is 0 USD and has no request id. The global limit stays unchanged, and ledger audit shows no paid request. Catalog schema is v23. Local IPC is v24. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 303 crate dependencies against 1,264 advisories. `cargo verify-media` was not rerun because the decoder and acquisition path are unchanged. See [local transcripts](../decisions/0032-local-transcripts.md).

Operation 18 is in place. `listen file` plays one sealed segment of a running capture, mapping the timeline seek onto that file. A seek in a gap fails. A seek at the open tail fails before the decoder starts. `listen attach` opens a playhead parked at the newest published end. `listen pause` stores it and does not signal the capture worker or write a gap. `listen seek` moves that playhead only inside a published segment. `listen live` parks at the newest published end. `listen play` decodes that segment and then drops the playhead. `listen detach` drops a playhead and leaves the capture running. Two playheads can name the same capture. A paused position earlier than the earliest retained start is expired until the listener seeks or returns to live. Sessions are in memory and are not resumed after restart. The local fixture plays segment 0 and segment 1 on one socket while the job stays running, then seeks, pauses, and detaches without a gap. The segment lease is two receive windows so the close can renew. An expired lease is still not extended, and the 5000 ms receive cut is unchanged. Catalog schema stays v18. Local IPC is v19. No application dependency was added. `cargo verify` passed formatting, workspace tests, warnings-denied Clippy, build, and dependency auditing of 286 crate dependencies against 1,261 advisories. `cargo verify-media` passed the twelve native-media tests, including that fixture. See [segment playback](../decisions/0027-segment-playback.md).

Optional [network routing](../design/network-routing.md) remains operation 40. Segment playback is recorded above; native inference workers remain planned; supervised checksum jobs reuse the existing catalog and worker path. The [broadcast analysis contract](../design/broadcast-analysis.md) still has no detector. Disposable diagnostics belong in ignored `.agents/`. Verify Git and remote state before claiming a clean or synchronized checkpoint. No release or deployment is implied by this record.
