# Hindi calibration audio pilot

Reviewed: 2026-09-23. One selected Hindi input from the frozen [three-clip pilot](three-clip-pilot.json) has passed full archive extraction and independent original-WAV inspection. It has not been decoded, recognized, translated or scored. The [pinned dataset attribution packet](fleurs-subset-attribution.md) supplies the card, CC BY 4.0 terms and selected identity.

The [Hindi archive transfer](hindi-archive-transfer-pilot.md) published the revision-pinned 1,256,867,167-byte `train.tar.gz` with publisher SHA-256 `bb6f52bfb27ca91c54539480111163eb9245305008cfe0526c6f4af3bfdd04e9`. A separate Hindi-only extractor verified its exact reservation, publication, HEAD evidence and terminal progress under the shared ledger lock, then rehashed the complete compressed file. It scanned the gzip and TAR through their ends, checking **2,121 entries** and **1,535,477,760 expanded bytes**. It retained only `train/2934636319415126502.wav`; no allowlisted archive notice member appeared. The extraction made no network request and left the 22-row acquisition ledger at **13,608,843,985 policy bytes**, including the historical 10 GiB charge. External paid spend remains USD 0.

The schema 2 completion receipt has phase `archive_verified_decode_pending`, SHA-256 `0a886d2d6b0b86304e86aabf48c44784fdf2c46e030cff476861d465b24babfb`. Its acquisition binding has SHA-256 `868ceb677a0072c944b096e26b9fcce4a7d1724da6bb7147a2bb295ae397ec8d`. The published extractor identity binds source aggregate `b6d578d75b5c5b5ddb54a47d48bdf2585a647792055f62e36be59ccdd66e9f7f`, binary `6837ee6044d55447ca053a9332b16a2d229de50e68858825d7ac89c1ea4a34cc`, and control `ede1f57494c56d02c446f80b42f496b1096e80da4d57f978f818a2c31853251c`. The receipt reports **145.672 seconds** for workspace admission, executable identity, local archive hash, full scan and staged writes, ending before its final receipt write. This is one local Windows observation, not a throughput or platform capacity claim.

A separate read-only audit matched the completion and acquisition hashes against the retained publications, found exactly the six expected candidate-state files, and confirmed that all 153 earlier retained evidence files, including the Arabic state, remained unchanged. It found no staging or failed file. This audit did not run the extractor or decoder.

The retained original WAV is **829,498 bytes**, SHA-256 `9dc7ccfaa8b1b36effb05289d138a03a1ef44e3ecf55872314c17b639b653da6`; independent file hashing matched the completion member. A separate bounded RIFF inspection found mono 16 kHz IEEE float32, an 18-byte `fmt ` chunk, a four-byte `fact` chunk declaring **207,360 samples**, and **829,440 data bytes**. The RIFF length and sample count agree on **12.96 seconds**. The inspector did not decode speech. Its script and JSON receipt remain in ignored evaluation storage.

The reference TSV stays evaluator-only, outside the worker input. The existing Arabic decoder is pinned to the Arabic extractor and cannot accept this new completion. A separately reviewed Hindi decoder must validate this exact original and publish a metadata-free PCM worker copy before recognition. The three-file end-to-end pilot remains open; no language-quality or iGPU result exists.
