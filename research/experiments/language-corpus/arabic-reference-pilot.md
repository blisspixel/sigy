# Arabic calibration reference pilot

Reviewed: 2026-09-23. This is one genuine, evaluator-only reference acquisition for the frozen [three-clip calibration pilot](three-clip-pilot.json). It is not audio, recognition, translation, or a language-quality result.

The source was the revision-pinned [`ar_eg` train TSV](https://huggingface.co/datasets/google/fleurs/blob/70bb2e84b976b7e960aa89f1c648e09c59f894dd/data/ar_eg/train.tsv). A separately reviewed route reserved its exact 1,528,714-byte size in the shared experiment ledger before DNS. One HTTPS GET returned HTTP 200 with that Content-Length and the previously observed quoted ETag `"9bd974c06f667ccce13920b023be0f34e91856c3"`. Redirects, proxy, retry, fallback, range, transfer coding, and automatic decompression were refused. The full raw body was published once with SHA-256 `416fbb37f2d4226b5e1e393dafa12920c5ea596cada371d115e3a1c13b9b8c8b`. The ETag is corroborating HTTP metadata; no independent Git blob identity is claimed.

Strict UTF-8 validation checked the byte-identical frozen text hash, complete row count, and the selected row's file identity, position, sample count, original-text hash and normalized-text hash. The selected reference hash is `eeb8c7854d162163f41b25ad7e9950f7f7b43d8ff140f84d1564607de54cd944` for `ar_eg/train/16932136382444340406.wav`. The reference string remains in ignored evaluator storage and is not supplied to the recognition worker or included here. The route descriptor SHA-256 is `6c9d729619b070692eaf6d2c091cb3ac1bf26bd67815e5d6cd11082111c28bdd`.

The acquisition published a reservation, payload receipt, strict-reference validation receipt and no-clobber file. A separate read-only `verify-reference` call reopened the shared ledger, rehashed the file, validated the selected row, and matched the publication receipt. Its output exposed provenance and hashes without the reference text. Replay of this object ID cannot issue a second request. The five earlier reservations remain charged, so the cumulative policy charge is **10,748,167,984 bytes** under the 100,000,000,000-byte ceiling; it includes the explicitly unmeasured historical 10 GiB policy charge. These are conservative reservations, not measured wire traffic. External paid spend remains USD 0.

This verifies one selected calibration reference. The associated [Arabic archive and waveform](arabic-audio-pilot.md) were acquired, extracted and decoded separately, with no reference text in that processing attempt. Recognizer output, scoring, and inference resource receipts remain open. The [Hindi reference](hindi-reference-pilot.md) was acquired later; Spanish and frozen holdout references remain unrequested. No result from this reference acquisition qualifies a language, accent, device, or release.
