# Pinned FLEURS card acquisition

Reviewed: 2026-09-23. This is provenance and license preparation for the frozen three-clip pilot, not audio or recognition evidence.

The source is the [FLEURS dataset card at revision `70bb2e84b976b7e960aa89f1c648e09c59f894dd`](https://huggingface.co/datasets/google/fleurs/blob/70bb2e84b976b7e960aa89f1c648e09c59f894dd/README.md). Two bounded attempts failed length admission before reading a body; their exact response headers were not retained. Their separate 65,536-byte and 65,537-byte reservations remain charged. A header-only, no-redirect request then observed HTTP 200, `Content-Length: 385614`, and ETag `"dcc0872174e54ad416dee938651d777378d4ba4f"`. That HEAD read no application body. It informed a third immutable route with an exact 385,614-byte reservation and matching GET length and ETag requirements.

The third route published 385,614 raw bytes in ignored local experiment storage. The retained SHA-256 is `688f79f2a5c731af3796e9f683eb02f9b3f09d040decd8c5625d0f37098e71c6`. `git hash-object --no-filters` independently produced Git blob ID `dcc0872174e54ad416dee938651d777378d4ba4f`, matching both observed ETags. The source revision URL, TLS connection, full length, matching ETag, local hashes, and publication receipt identify this copy. Links and code examples inside the card cannot authorize new requests, code execution, or budget changes.

The pinned card's YAML declares `cc-by-4.0`; its prose links a generic CC-BY page. It supplies the Conneau et al. (2022) FLEURS citation and contributor acknowledgments. Preserve the raw card and those third-party credits. The supplied BibTeX block lacks its final closing brace, so a separately formatted citation may repair that syntax without changing the retained source. Do not infer a named copyright holder from creator-category metadata.

The [official CC BY 4.0 legal text](https://creativecommons.org/licenses/by/4.0/legalcode.txt) was retained through a separately bounded route on 2026-09-23. The fixed URL returned 18,657 bytes with local SHA-256 `9ba9550ad48438d0836ddab3da480b3b69ffa0aac7b7878b5a0039e7ab429411`. The route reserved 131,073 bytes before the request; its weak HEAD ETag is informational, and the URL does not pin an immutable upstream revision. The local copy begins with "Attribution 4.0 International" and includes Section 3's attribution conditions. The [pilot attribution packet](fleurs-subset-attribution.md) records the title, supplied citation and acknowledgments, pinned source and revision, verified card hashes, license link, selected clip IDs, and selection changes. Preserve any additional notices found after acquiring an archive and record later processing changes. Hosted processing rights and terms need a separate review. No selected audio, model, or runtime object has been downloaded or run.

The shared experiment ledger charges 10,738,066,000 bytes after these four attempts, including an explicitly unmeasured historical 10 GiB policy charge. It leaves 89,261,934,000 bytes under the 100,000,000,000-byte ceiling. Those charges are conservative reservations, not measured wire traffic. The first two attempts returned no application body bytes; the third returned the full 385,614-byte card, and the fourth returned the 18,657-byte legal text. Do not quote a 64 KiB card size from the two failed caps.
