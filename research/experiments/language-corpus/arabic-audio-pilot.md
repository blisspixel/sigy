# Arabic calibration audio pilot

Reviewed: 2026-09-23. This is one acquired and extracted input for the frozen [three-clip pilot](three-clip-pilot.json), not a language-quality result. The [pinned FLEURS card and attribution packet](fleurs-subset-attribution.md) provide the dataset credit and CC BY 4.0 terms. The retained archive, selected audio, receipts, and working binaries are in ignored evaluation storage, outside worker reference inputs.

The Arabic `train.tar.gz` came from `google/fleurs` revision `70bb2e84b976b7e960aa89f1c648e09c59f894dd` through one reviewed HTTPS route. The shared ledger reserved its full **1,113,698,891 bytes** before DNS. One GET completed without an asset retry and published the exact expected SHA-256 `b19fa9435c0d7587bf0ede7c9da134567b2eb55a06e2e7523547f0a6fcb3eef7`. A separate local hash and size check agreed. The reservation, publication and terminal progress receipts bind the same immutable route descriptor. The policy ledger now has eleven reservations and **11,861,880,458 bytes** charged under the 100,000,000,000-byte cumulative ceiling. That includes an explicitly unmeasured historical 10 GiB policy charge; it is not measured wire traffic. Paid spend is USD 0.

The separately frozen Arabic-only extractor admitted those receipts under the shared ledger lock, verified the whole compressed hash again, and scanned the gzip and TAR through their ends. It checked **2,105 entries** and **1,395,752,960 expanded bytes**, retaining only `train/16932136382444340406.wav`. Its completion receipt reports **91.806 seconds** for admission, local hash and scan work, excluding its final receipt write. The retained WAV is **660,538 bytes**, SHA-256 `3d6ea5d17f75e59823f6f0d5b4641875d278074e53a71c8fee20d8ac2b54962e`. Independent header inspection finds mono 16 kHz IEEE float32, a four-byte `fact` chunk declaring **165,120 samples**, and 660,480 data bytes. The exact RIFF length and sample count correspond to **10.32 seconds**. No archive notice member was present in this extraction; the retained card, legal text and attribution packet remain the provenance basis.

An Arabic-only decoder then read the exact extractor completion and original WAV. The installed FFmpeg 9.0.1 executable was independently hashed and received verified audio bytes on a private pipe, with a WAV demuxer and PCM decoder allowlist. The decoder required the float32 `fact` count to match the data length and frozen sample count. It published a metadata-free mono PCM16 worker WAV of **330,284 bytes**, including a 44-byte header, SHA-256 `260c1b66074644150e7885a899ab1049831060b155eb05ee58f18901359bcd5b`. Independent file hashing matched the completion receipt. The child produced exactly 330,240 PCM bytes, zero stderr bytes, and exited with zero job members remaining. Its receipt reports **42.219 ms** child-process elapsed time, **31.25 ms** job CPU time and **20,963,328 bytes** peak job committed memory; the full guarded decode path, including workspace admission and executable hashing, took **5.317 seconds**. These are one Windows-host observation, not sustained throughput, GPU use, RSS or a platform capacity result. No model was started.

The [Arabic reference TSV](arabic-reference-pilot.md) and selected row were validated separately and remain evaluator-only. Recognition workers must receive the opaque clip ID and decoded audio, not the reference, sentence group or source filename. The current worker directory separation is not yet qualified access control for native inference. Recognition, scoring, translation, other pilot clips and holdout audio are separate gates. This one decoded file does not advance the three-file end-to-end stage or establish broadcast performance, language support, no-network inference, or platform capacity.
